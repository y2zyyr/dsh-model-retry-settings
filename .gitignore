# Dependencies and package-manager state
node_modules/

# Build output (regenerated by scripts/build.mjs)
lib/
client.js

# DSH runtime data and backups — never publish
.dsh/
*.bak

pnpm-workspace.yaml

*.tgz

# Test/build output and logs
coverage/
.nyc_output/
*.log

# Local editor/OS files
.DS_Store
.idea/
.vscode/
*.swp

# Local agent project context — this file is LOCAL ONLY and must never be
# committed, pushed, or uploaded (see AGENTS.md).
AGENTS.md